using UnityEngine;
using System.Collections;

namespace TB
{
    public enum PlayerType
    {
        Tetris,
        Digger,
    }

    public enum PhotonEvent
    {
        DestroyBlock = 1,
    }
}
